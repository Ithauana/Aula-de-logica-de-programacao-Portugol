programa {
  real salario_bruto = 0
  real salario_desconto

  funcao inicio() {
    escreva("\nDigite o salário bruto")
    leia(salario_bruto)

    se(salario_bruto <= 2000){
      salario_desconto = salario_bruto-(salario_bruto * 0.08)
      escreva(salario_desconto)
    } senao {
      salario_desconto = salario_bruto - (salario_bruto * 0.10)
    }

    se sala
  }
}
